import { Leon } from "./export.js";

export class Leon extends Animal {
    constructor(nombre, edad, comentarios, imagen, sonido) {
        super(nombre, edad, comentarios, imagen, sonido);
    }
}

import { Lobo } from "./export.js";

export class Lobo extends Animal {
    constructor(nombre, edad, comentarios, imagen, sonido) {
        super(nombre, edad, comentarios, imagen, sonido);
    }
}
import {Oso} from "./export.js"
export class Oso extends Animal {
    constructor(nombre, edad, comentarios, imagen, sonido) {
        super(nombre, edad, comentarios, imagen, sonido);
    }
}
import { Serpiente } from "./export.js";
export class Serpiente extends Animal {
    constructor(nombre, edad, comentarios, imagen, sonido) {
        super(nombre, edad, comentarios, imagen, sonido);
    }
}
import { Aguila } from "./export.js";
export class Aguila extends Animal {
    constructor(nombre, edad, comentarios, imagen, sonido) {
        super(nombre, edad, comentarios, imagen, sonido);
    }
}